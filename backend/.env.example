# Example Environment Variables
# Copy this file to create environment-specific configurations:
# - .env.local (for local development with DynamoDB Local)
# - .env.development (for AWS development environment)
# - .env.staging (for AWS staging environment)
# - .env.production (for AWS production environment)

# AWS Configuration
AWS_REGION=us-east-1

# DynamoDB Configuration
# For local development, use DynamoDB Local endpoint
DYNAMODB_ENDPOINT=http://localhost:8000

# For AWS environments, omit DYNAMODB_ENDPOINT and use table names from CDK
EVENTS_TABLE=LiveQuizEvents
QUESTIONS_TABLE=LiveQuizQuestions
PARTICIPANTS_TABLE=LiveQuizParticipants
ANSWERS_TABLE=LiveQuizAnswers
GAME_PINS_TABLE=LiveQuizGamePins

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Set to your frontend URL
CORS_ORIGIN=http://localhost:5173

# AWS Credentials (for local development only)
# In production, use IAM roles instead
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# Question Images Configuration
# S3 bucket for storing question images
QUESTION_IMAGES_BUCKET=live-quiz-question-images-ACCOUNT_ID
# CloudFront URL for serving images
CLOUDFRONT_IMAGES_URL=https://xxxxx.cloudfront.net
