# Production Environment Variables (AWS)

# AWS Configuration
AWS_REGION=us-east-1

# DynamoDB Table Names (will be set from CDK stack outputs)
EVENTS_TABLE=LiveQuizEvents
QUESTIONS_TABLE=LiveQuizQuestions
PARTICIPANTS_TABLE=LiveQuizParticipants
ANSWERS_TABLE=LiveQuizAnswers
GAME_PINS_TABLE=LiveQuizGamePins

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS (allow production frontend - will be set from CloudFront URL)
CORS_ORIGIN=https://example.com
